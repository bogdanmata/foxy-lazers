query SelectRequestedSecLendingContract {
  description: "Select all SecurityLendingContract requested"
  statement:
      SELECT com.rbc.hackathon.SecurityLendingContract
        WHERE (status == 'REQUESTED')
}
