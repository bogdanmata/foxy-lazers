import {FinalCountdownPipe} from './final-countdown.pipe';

describe('FinalCountdownPipe', () => {
  it('create an instance', () => {
    const pipe = new FinalCountdownPipe();
    expect(pipe).toBeTruthy();
  });
});
